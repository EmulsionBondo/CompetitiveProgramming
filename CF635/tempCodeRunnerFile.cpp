for(int i=1;i<=nr;i++){
    //     auto bitr = lower_bound(B.begin(), B.end(), R[i]);
    //     auto gitr = lower_bound(G.begin(), G.end(), R[i]);
    //     ll tmp = 0;
    //     tmp += (R[i] - *bitr) * (R[i] - *bitr);
    //     tmp += (R[i] - *gitr) * (R[i] - *gitr);
    //     tmp += (*gitr - *bitr) * (*gitr - *bitr);
    //     chmin(ans, tmp);
    // }